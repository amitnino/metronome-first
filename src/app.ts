// Imports

import { Timer, TimerInterface } from './accurateTimer';

// DOM Elements

import {
    tempoText
} from './htmlElements';

// Variables

/** @type {Number} */
let bpm: Number = 140;

/** @type {Number} */
let beatsPerMeasure: Number = 4;

tempoText.textContent = '159';